### 논리 볼륨 개요

- 물리 장치
- PV (물리 볼륨, Physical Volume)
- VG (볼륨 그룹, Volume Group)
- LV (논리 볼륨, Logical Volume)

<hr>

### 논리 볼륨 생성

**파티션/물리 볼륨 생성**
- `parted -s /dev/sda mklabel gpt mkpart primary 1MiB 769MiB`
- `parted /dev/sda mkpart primary 769MiB 1000MiB`
- `parted /dev/sda set 1 lvm on`
- `parted /dev/sda set 2 lvm on`
- `udevadm settle`

**볼륨 그룹 생성**
- `vgcreate vg01 /dev/sdb1 /dev/sdb2 -s 16M` (`-s` : PE 크기 변경, 시험 암기!!)

**논리 볼륨 생성**
- `lvcreate -n lv01 -L 128M vg01`
- `mkfs -t xfs /dev/vg01/lv01`

**`/etc/fstab` 추가**
- `/dev/vg01/lv01 /mnt/data xfs defaults 0 0`

<hr>

### 볼륨 그룹 확장

- `pvcreate /dev/sdb3`
- `vgextend vg01 /dev/sdb3`

<hr>

### 볼륨 그룹 축소

- `pvmove /dev/sdb1`
- `vgreduce vg01 /dev/sdb1`

<hr>

### 논리 볼륨 확장

- `xfs` : 보통 확장만 가능 (축소 불가)
- `ext4` : 축소, 확장 모두 가능

**명령어**
- `lvextend -r -L +500M /dev/vg01/lv01`
  - `-r` : 파일 시스템 리사이즈까지 함께 수행
- `-r` 생략한 경우 별도로 리사이즈 필요
  - `xfs_growfs /mnt/mount` (마운트 포인트 지정)
  - `resize2fs /dev/vg01/lv01` (LV 경로 지정, ext 계열)

<hr>

### 논리 볼륨 제거

- `umount`, `/etc/fstab` 항목 제거
- `lvremove /dev/vg01/lv01`
- `vgremove vg01`
- `pvremove /dev/sdb2 /dev/sdb3`

<hr>

### 스왑 공간 논리 볼륨

- `swapoff -v /dev/vg01/swap`
- `mkswap /dev/vg01/swap`
- `swapon /dev/vg01/swap`