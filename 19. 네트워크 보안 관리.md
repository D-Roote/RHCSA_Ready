### 방화벽 서비스

- `firewalld` 서비스 : 동적 방화벽 관리자

<hr>

### firewalld 사전 정의된 영역

- `trusted` : 들어오는 모든 트래픽 허용 (가장 개방적)
- `home` : 홈 네트워크, 대부분의 서비스 신뢰
- `internal` : 내부 네트워크, `home`과 유사하게 신뢰
- `work` : 업무 네트워크, 대부분의 컴퓨터 신뢰
- `public` : 공용 네트워크, 다른 컴퓨터 신뢰하지 않음 (기본 영역)
- `external` : 외부 네트워크, 라우터의 마스커레이딩 활성화 상태
- `dmz` : 공개 접근 가능한 서비스만 제한적으로 허용
- `block` : 들어오는 연결 거부 (ICMP 응답으로 거부 알림), 나가는 연결은 허용
- `drop` : 들어오는 연결을 응답 없이 그냥 버림 (가장 폐쇄적)

<hr>

### 명령줄 방화벽 구성

- `firewall-cmd --help`
- `firewall-cmd --get-default-zone`
- `firewall-cmd --list-all`
- `firewall-cmd --add-service=http --permanent`
- `firewall-cmd --reload`

<hr>

### SELinux 포트 레이블 지정

- 14장 참조