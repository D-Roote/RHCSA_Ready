파일 생성
 - touch 파일이름
 - 추가 활용
    {1..3} : 1,2,3 생성

디렉터리 생성
 - mkdir 경로/디렉터리이름
 - 옵션
    -p : 상위 경로 미존재시 같이 생성

파일 및 디렉터리 복사
 - cp 대상 목표
 - 옵션
    -p : 권한, 소유권 등 보존하여 복사
    -r : 디렉터리 복사

파일 및 디렉터리 이동
 - mv 대상 목표

파일 및 디렉터리 제거
 - rm 파일/디렉터리
 - 옵션
    -r : 디렉터리 삭제
    -f : 묻지 않고 강제 삭제

링크 생성
 - ln 대상 목표 (하드 링크)
 - 옵션
    -s : 소프트/심볼릭 링크 생성
 - 하드 링크 : 동일한 inode 공유, 하나의 파일을 삭제해도 남아있음 (단, 디렉터리 및 다른 파일시스템 간에는 생성 불가)
 - 소프트 링크 : 경로에 대한 참조 생성, 원본 파일을 삭제하면 참조가 깨짐 (dangling link)

자주 쓰이는 bash 쉘 패턴 일치

 - 글롭(와일드카드) 확장
    * : 0개 이상의 문자로 이루어진 모든 문자열
    ? : 모든 단일 문자
    [abc...] : 대괄호에 포함된 한 문자
    [!abc...] 또는 [^abc...] : 대괄호에 포함되지 않은 한 문자

 - 중괄호 확장
    {a..c} : 범위에 포함되는 문자 시퀀스 생성

 - 틸드 확장
    ~ : 사용자의 홈 디렉토리
    ~USER : USER의 홈 디렉토리

 - 변수 확장
    VAR=variable >> 사용 : $VAR

 - 명령 대체
    $(CMD)
    `CMD`

 - 인수 확장 방지 (Quoting)
    'CMD' : 모든 확장 방지 (완전한 리터럴)
    "CMD" : 변수 확장($VAR)과 명령 대체($(...))는 허용, 글롭 확장은 방지
    "\$HOME" : 큰따옴표 안에서 백슬래시로 $ 이스케이프 → 문자 그대로 $HOME 출력