시험 경향 핵심
간단한 systemd 만들기

중요
systemctl enable --now UNIT
systemctl restart UNIT

systemd 데몬
 - systemd 데몬 : Linux 시작 프로세스를 관리
 - 데몬 : 백그라운드에서 대기하거나 실행되어 다양한 작업을 수행하는 프로세스

서비스 유닛 나열
 $ systemctl list-units --type=service
 - -all 옵션 : 활성화 상태와 관계없이 모드 서비스 유닛 표시

 - ACITVE 컬럼
    active : 현재 활성 상태
    inactive : 비활성 상태
    failed : 실행 실패
    activating : 활성화 중
    deactivating : 비활성화 중

 - SUB 컬럼값
    running : 실행 중
    dead : 정지된 상태
    exited : 실행 후 종료됨
    waiting : 대기중
    listening : 요청 대기중
    mounted : 파일 시스템이 마운트 됨

 $ systemctl list-unit-files --type=service

  - STATE
    enabled : 시스템 시작 시 자동 실행
    disabled : 자동 실행 X
    static : 단발성 실행 

서비스 상태 보기
 $ systemctl status UNIT
 systemctl 명령어
    $ is-active UNIT
    $ is_enabled UNIT
    $ is_failed UNIT >> systemctl --failed --type=(type)

!! 중요 - 시스템 서비스 제어
 - 서비스 제어
    $ systemctl enable --now UNIT
    $ systemctl restart UNIT
    $ systemctl reload-or-restart UNIT

 - 유닛 종속성 나열
    $ systemctl list-dependencies UNIT (--reverse : 역방향 종속성)

