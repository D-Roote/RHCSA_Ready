### systemd 데몬

- systemd 데몬 : Linux 시작 프로세스를 관리
- 데몬 : 백그라운드에서 대기하거나 실행되어 다양한 작업을 수행하는 프로세스

<hr>

### 서비스 유닛 나열

- `systemctl list-units --type=service`
- `--all` 옵션 : 활성화 상태와 관계없이 모든 서비스 유닛 표시

**ACTIVE 컬럼**
- `active` : 현재 활성 상태
- `inactive` : 비활성 상태
- `failed` : 실행 실패
- `activating` : 활성화 중
- `deactivating` : 비활성화 중

**SUB 컬럼값**
- `running` : 실행 중
- `dead` : 정지된 상태
- `exited` : 실행 후 종료됨
- `waiting` : 대기 중
- `listening` : 요청 대기 중
- `mounted` : 파일 시스템이 마운트됨

<hr>

### 서비스 유닛 파일 나열

- `systemctl list-unit-files --type=service`

**STATE**
- `enabled` : 시스템 시작 시 자동 실행
- `disabled` : 자동 실행 안 함
- `static` : 다른 유닛에 의해 종속적으로만 활성화 (직접 활성화 목적 아님)

<hr>

### 서비스 상태 보기

- `systemctl status UNIT`

**관련 systemctl 명령어**
- `systemctl is-active UNIT`
- `systemctl is-enabled UNIT`
- `systemctl is-failed UNIT`
- `systemctl --failed --type=service` : 실패한 서비스 유닛 전체 목록 확인

<hr>

### !! 중요 - 시스템 서비스 제어

**서비스 제어**
- `systemctl enable --now UNIT` : 부팅 시 자동 시작 설정 + 즉시 시작
- `systemctl restart UNIT` : 서비스 재시작
- `systemctl reload-or-restart UNIT` : 재로드 지원 시 재로드, 미지원 시 재시작

**유닛 종속성 나열**
- `systemctl list-dependencies UNIT`
  - `--reverse` : 역방향 종속성 확인