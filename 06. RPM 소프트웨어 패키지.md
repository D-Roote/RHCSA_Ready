### RPM 패키지 검사

- `rpm -q 패키지` : 현재 설치된 패키지 버전 표시
- `rpm -qa` : 설치된 패키지 모두 표시
- `rpm -qf FILENAME` : FILENAME을 제공하는 패키지 확인

<hr>

### RPM 패키지 추출

- `rpm2cpio 패키지 | cpio -tv` : RPM 패키지 안의 파일 목록 표시
- `rpm2cpio 패키지 | cpio -idv "파일"` : 특정 파일 추출

<hr>

### DNF 소프트웨어 찾기

- `dnf list` : 설치되어 있거나 사용 가능한 패키지 표시
- `dnf search all KEYWORD` : 키워드만으로 패키지 검색
- `dnf info PACKAGE` : 패키지 정보 반환

<hr>

### DNF 소프트웨어 설치, 제거

- `dnf install -y PACKAGE`
- `dnf remove PACKAGE`

<hr>

### DNF 소프트웨어 그룹 찾기

- `dnf group list`
- `dnf group info GROUP`

<hr>

### DNF 소프트웨어 그룹 설치, 제거

- `dnf group install -y GROUP`
- `dnf group remove GROUP`

<hr>

### 소프트웨어 리포지토리 관리

- `dnf repolist all`
- `dnf config-manager --enable REPO` / `dnf config-manager --disable REPO`

**구성 파일**
- `/etc/yum.repos.d/*.repo`
- `/etc/dnf/dnf.conf`

**리포지토리 추가**
- `dnf config-manager --add-repo="주소"`