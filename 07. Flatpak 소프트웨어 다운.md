### Flatpak 사용

- `dnf install flatpak`
- `podman login registry.redhat.io`
- `flatpak remotes (-d) (--system/--user)` : 원격 리포지토리 반환
- `flatpak remote-ls --app NAME` : 원격 리포지토리에서 사용할 수 있는 애플리케이션만 나열

<hr>

### Flatpak 원격 리포지토리 추가, 변경, 제거

- `flatpak remote-add --if-not-exists (--user) NAME LINK`
- `flatpak remote-modify --enable NAME` / `flatpak remote-modify --disable NAME`
- `flatpak remote-delete NAME`

<hr>

### Flatpak에서 애플리케이션 설치

- `flatpak -v search APP`
- `flatpak install NAME APP`