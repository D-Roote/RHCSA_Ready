### 파일/디렉토리 사용자 정보 표시

- 파일 : `ls -l 파일`
- 디렉토리 : `ls -ld 디렉토리`

<hr>

### 로컬 사용자 계정

- 사용자 정보 : `/etc/passwd`
- 그룹 정보 : `/etc/group`
- 비밀번호(암호화) 정보 : `/etc/shadow`

<hr>

### 사용자 계정 전환

- `su USER` : 비로그인 쉘로 전환 (환경변수 미적용)
- `su - USER` : 로그인 쉘로 전환 (해당 사용자의 환경변수 적용)
- `su -` : 슈퍼유저(root)로 전환
- `su - USER` : USER 사용자로 전환

<hr>

### 관리자 명령 실행

- `sudo CMD`

<hr>

### 대화형 루트 쉘

- `sudo -i`

<hr>

### Sudo 구성

- `visudo`로 수정하기 (문법 오류 검증 후 저장)
- 경로
  - `/etc/sudoers`, `/etc/sudoers.d/*`
- 시험 주요 예시
  - `USER    ALL=(ALL)   ALL` : USER가 모든 명령 실행 가능
  - `%GROUP  ALL=(ALL)   NOPASSWD: ALL` : GROUP 그룹 사용자가 비밀번호 없이 모든 명령 실행 가능
  - (그룹 지정 시 `%` 접두사 필수)

<hr>

### 로컬 사용자 관리

**사용자 생성**
- `useradd 사용자명`

**옵션 (useradd)**
- `-c "설명"` : 계정 설명(comment) 지정
- `-d 경로` : 홈 디렉토리 경로 지정
- `-e YYYY-MM-DD` : 계정 만료일 지정
- `-f 일수` : 비밀번호 만료 후 계정 비활성화까지 유예 일수
- `-g 그룹` : 기본(주) 그룹 지정
- `-G 그룹1,그룹2` : 보조 그룹 지정
- `-m` : 홈 디렉토리 생성
- `-M` : 홈 디렉토리 생성하지 않음
- `-N` : 개인 그룹(UPG) 생성하지 않음
- `-r` : 시스템 계정 생성 (UID 낮은 범위)
- `-s 쉘경로` : 로그인 쉘 지정
- `-u UID` : UID 직접 지정

**비밀번호 변경**
- `passwd 사용자명` (대화형으로 변경)
- `echo "PASSWD" | passwd --stdin 사용자명`
- `echo "사용자명:PASSWD" | chpasswd` (권장, 스크립트/자동화에 적합)

**옵션 (passwd)**
- `-l` : 계정 잠금 (lock)
- `-u` : 계정 잠금 해제 (unlock)
- `-d` : 비밀번호 삭제 (빈 비밀번호로 설정)
- `-e` : 비밀번호 즉시 만료 처리 (다음 로그인 시 변경 강제)
- `-S` : 계정 상태 확인 (요약 정보)
- `--stdin` : 표준입력으로 비밀번호 전달

**사용자 수정**
- `usermod`

**옵션 (usermod)**
- `-c "설명"` : 계정 설명 변경
- `-d 경로 [-m]` : 홈 디렉토리 변경 (`-m`과 함께 쓰면 기존 데이터도 이동)
- `-e YYYY-MM-DD` : 계정 만료일 변경
- `-g 그룹` : 기본(주) 그룹 변경
- `-G 그룹1,그룹2` : 보조 그룹 변경 (기존 보조 그룹 목록을 덮어씀)
- `-aG 그룹` : 기존 보조 그룹은 유지한 채 그룹 추가 (반드시 `-a`와 `-G` 함께 사용)
- `-l 새이름` : 로그인명(사용자명) 변경
- `-L` : 계정 잠금
- `-U` : 계정 잠금 해제
- `-s 쉘경로` : 로그인 쉘 변경
- `-u UID` : UID 변경

**사용자 삭제**
- `userdel 사용자명`

**옵션 (userdel)**
- `-r` : 홈 디렉토리 및 메일 스풀까지 함께 삭제
- `-f` : 사용자가 로그인 중이거나 파일을 소유해도 강제 삭제

<hr>

### 로컬 그룹 관리

**그룹 생성**
- `groupadd 그룹명`

**옵션 (groupadd)**
- `-g GID` : GID 직접 지정
- `-r` : 시스템 그룹 생성

**그룹 수정**
- `groupmod`

**옵션 (groupmod)**
- `-n 새그룹명` : 그룹명 변경
- `-g GID` : GID 변경

**그룹 삭제**
- `groupdel 그룹명`
- (단, 삭제하려는 그룹이 어떤 사용자의 기본(주) 그룹으로 지정되어 있으면 삭제 불가)

<hr>

### 비밀번호 정책 기본값 (`/etc/login.defs`)

- 역할 : `useradd`로 **새 계정 생성 시** 적용되는 비밀번호 정책 기본값을 정의 (기존 계정에는 영향 없음, 기존 계정은 `chage`로 개별 수정)
- 주요 필드
  - `PASS_MAX_DAYS` : 비밀번호 최대 사용 기간 (일)
  - `PASS_MIN_DAYS` : 비밀번호 최소 사용 기간 (일, 변경 후 재변경까지 대기 일수)
  - `PASS_WARN_AGE` : 만료 전 경고 시작 일수
  - `PASS_MIN_LEN` : 비밀번호 최소 길이 (일부 배포판에서는 PAM으로 대체되어 미사용)
  - `UID_MIN`, `UID_MAX` : 일반 사용자 UID 범위
  - `SYS_UID_MIN`, `SYS_UID_MAX` : 시스템 계정 UID 범위
- 시험 주요 예제
  - `grep PASS_MAX_DAYS /etc/login.defs` : 현재 기본 최대 사용 기간 확인
  - `PASS_MAX_DAYS   90` : 값 직접 수정 (vi/vim 등으로 편집)
  - `chage -l 사용자명` : 특정 사용자에게 적용된 실제 정책 값 확인 (`/etc/shadow`에 개별 저장된 값 기준)

<hr>

### 사용자 암호 관리

- 관리 파일 : `/etc/shadow`
- 필드 :
  - 사용자명 : 암호화 비밀번호 : 마지막 변경일 : 최소 사용 기간 : 최대 사용 기간 : 만료 경고 기간 : 비활성화 유예 기간 : 계정 만료일 : 예약 필드
- 관리 명령어 : `chage` (Change Age)

**옵션 (chage)**
- `-l 사용자명` : 현재 비밀번호 만료 정보 조회
- `-d YYYY-MM-DD` (또는 `-d 0`) : 마지막 변경일 설정 (0이면 다음 로그인 시 변경 강제)
- `-m 일수` : 최소 사용 기간 설정
- `-M 일수` : 최대 사용 기간 설정
- `-W 일수` : 만료 경고 기간 설정
- `-I 일수` : 만료 후 비활성화까지 유예 기간 설정
- `-E YYYY-MM-DD` : 계정 만료일 설정

**시험 주요 예제**
- `chage -E $(date -d "+30 days" +%F) 사용자명` (계정 만료일 설정)
- `chage -d 0 사용자명` (다음 로그인 시 비밀번호 변경 강제)
- 나머지 예제는 `chage --help` 참조