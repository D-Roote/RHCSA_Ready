### 복구 미디어 사용

- GRUB2 메뉴 - Troubleshooting > Rescue a Red Hat Enterprise Linux system

**절차**
- `chroot /mnt/sysroot`
- `passwd root`
- `touch /.autorelabel`

<hr>

### 복구 미디어 미사용

- GRUB2 커널 편집
  - `linux~` 줄에서 `console=` 옵션 제거
  - `init=/bin/bash` 추가
  - 재부팅

**절차**
- `mount -o rw,remount /`
- `passwd`
- `touch /.autorelabel`
- `exec /sbin/init`