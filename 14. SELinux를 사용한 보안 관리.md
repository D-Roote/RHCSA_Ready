### SELinux 아키텍처

- 정의 : 특정 프로세스가 관련 파일, 디렉터리, 포트에 액세스하는 방법을 정의하는 보안 규칙
- 타깃 정책 : 하나의 정책이 애플리케이션 하나의 활동을 정의

<hr>

### SELinux 사용

**구성 파일**
- `/etc/selinux/config`
  - `SELINUX=enforcing/permissive/disabled`

**사용 명령어**
- `getenforce` : 현재 모드 확인
- `setenforce 1` / `setenforce 0` : Enforcing / Permissive 즉시 전환 (재부팅 시 초기화됨, 영구 적용은 config 파일 수정 필요)

<hr>

### SELinux 파일 컨텍스트 제어

**SELinux 컨텍스트**
- `user:role:type:level`
  - `_u` : SELinux User
  - `_r` : SELinux Role
  - `_t` : SELinux Type
  - `s0` : MLS/MCS Security Level

**명령어**
- `semanage fcontext -l | grep 디렉토리`
- `semanage fcontext -a -t httpd_sys_content_t '/virtual(/.*)?'` : 컨텍스트 규칙 추가
- `restorecon -Rv /virtual` : 정의된 규칙대로 실제 컨텍스트 복원/적용
- `chcon -Rt httpd_sys_content_t /virtual` : 컨텍스트 임시 변경 (재레이블링 시 초기화될 수 있음, 영구 적용은 `semanage fcontext` + `restorecon` 권장)

<hr>

### SELinux Boolean

- 특정 서비스나 기능에 대해 미리 정의된 보안 동작을, 간단한 사용 여부(on/off)만으로 활성화·비활성화할 수 있도록 제공되는 기능

**명령어**
- `getsebool -a | grep httpd_enable_homedirs`
- `semanage boolean -l | grep httpd_enable_homedirs`
- `setsebool -P httpd_enable_homedirs on` (`-P` : 정책 파일에 영구 반영)

<hr>

### SELinux Port

- `semanage port -l | grep http_port_t`
- `semanage port -a -t http_port_t -p tcp 82`

<hr>

### SELinux 위반 모니터링

**`/var/log/messages`**
- `cat /var/log/messages | grep -i 'SELinux is preventing'`
- `sealert -l UUID`

**`/var/log/audit/audit.log`**
- `cat /var/log/audit/audit.log | grep 'avc:  denied'`
- `sealert -a /var/log/audit/audit.log`