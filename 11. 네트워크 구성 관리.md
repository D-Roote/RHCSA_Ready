명령줄에서 네트워킹 구성 (시험에서는 비추천, nmtui 사용)

 - 네트워크 장치 및 연결 관리
    서비스 구성 파일 : /etc/NetworkManager/system-connections/*

 - 네트워크 정보 보기
    $ nmcli dev status
    $ nmcli con show

 - 네트워크 연결 추가
    $ nmcli con add ifname (DEV) type ethernet

 - 네트워크 연결
    $ nmcli con up (DEV)
    $ nmcli con dis (DEV) : 연결 끊기(down 아님)
    $ nmcli con del (DEV) : 연결 삭제

    