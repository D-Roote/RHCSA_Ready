### 명령줄에서 네트워킹 구성

> 시험에서는 비추천, `nmtui` 사용 권장

<hr>

### 네트워크 장치 및 연결 관리

- 서비스 구성 파일 : `/etc/NetworkManager/system-connections/*`

<hr>

### 네트워크 정보 보기

- `nmcli dev status` : 장치(디바이스) 상태 확인
- `nmcli con show` : 연결(커넥션) 목록 확인

<hr>

### 네트워크 연결 추가

- `nmcli con add ifname DEV type ethernet`

<hr>

### 네트워크 연결 제어

- `nmcli con up DEV` : 연결 활성화
- `nmcli con down DEV` : 연결 비활성화
- `nmcli con delete DEV` : 연결 삭제
- `nmcli device disconnect DEV` : 장치(device) 자체를 비활성화