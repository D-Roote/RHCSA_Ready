### NFS 파일 시스템 마운트

- `mount` 명령을 사용하여 수동으로 마운트
- `/etc/fstab` 파일을 사용하여 영구적으로 마운트
  - 네트워크 문제 발생 시 스토리지 시간 제한 증가
- `autofs` 서비스를 사용하여 정적으로 자동 마운트

<hr>

### NFS 디렉토리 액세스

**설치**
- `dnf install -y nfs-utils` (시험 환경에는 설치되어 있지 않음)

**NFS 디렉토리 쿼리**
- NFSv3
  - `showmount -e server`
- NFSv4
  - `mount server:/ /mnt`
  - `tree /mnt` 로 공유 디렉토리 확인
  - `umount /mnt`

**NFS 디렉토리 수동 마운트**
- `mkdir mountpoint`
- `mount -o rw,sync server:/export mountpoint`

**NFS 디렉토리 영구 마운트**
- `/etc/fstab`
  - `server:/export mountpoint nfs rw(defaults) 0 0`

<hr>

### 자동 마운터

- 자동 마운터 이점 : 파일 시스템을 더 이상 사용하지 않을 때 자동으로 마운트 해제

**Map 파일**
- Master Map 파일 : `/etc/auto.master` (`/etc/auto.master.d/*.autofs`)
- Direct Map 파일 : 액세스 가능한 폴더(`/etc` 등)에 자유롭게 구성
- Indirect Map 파일 : 액세스 가능한 폴더(`/etc` 등)에 자유롭게 구성

**마운트 방식 차이**
- 직접 마운트 : `autofs` 서비스 시작 시 마운트 포인트가 자동으로 생성됨
- 간접 마운트 : 마스터맵의 부모 경로는 `autofs` 서비스 시작 시 생성됨

<hr>

### 자동 마운터 서비스 구성

**설치**
- `dnf install -y autofs nfs-utils`

**직접 마운트 방식**
- `/etc/auto.master` (`/etc/auto.master.d/*.autofs`)
  - `/-    /etc/auto.direct`
- `/etc/auto.direct`
  - `/mnt/docs   -rw,sync    hosta:/shares/docs`
  - 결과 = `mount hosta:/shares/docs /mnt/docs`

**간접 마운트 방식**
- `/etc/auto.master`
  - `/shares   /etc/auto.indirect`
- `/etc/auto.indirect`
  - `work    -rw,sync    hosta:/shares/work`
  - 결과 = `mount hosta:/shares/work /shares/work`

<hr>

### 맵 파일 와일드카드

- `* -rw,sync     hosta:/shares/&` (간접 마운트 방식만 사용 가능)
  - 결과 = `mount hosta:/shares/*` 와 `/shares/*` 매핑 (`*` = `&`)

<hr>

### 대체 자동 마운트 메서드 (권장 X)

- `/etc/fstab`
  - `hosta:/shares/proja /data nfs defaults,x-systemd.automount 0 0`