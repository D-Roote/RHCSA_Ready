### 디스크 파티셔닝

**MBR (Master Boot Record)**
- primary partition
- extended partition
- logical partition (extended partition 안에서만 생성 가능, 최대 11개, 5~15번)
- 2 TiB의 디스크 및 파티션 크기 제한

**GPT (GUID Partition Table)**
- 최대 128개의 파티션 제공
- 80억 TiB의 디스크 및 파티션 지원

<hr>

### 파티션 관리

**디스크 레이블 초기화 전에 실행할 명령어**
- `parted /dev/sdb print` : 기존 레이블 설정 확인
  - 이미 `msdos` 또는 `gpt` 레이블이 설정된 경우, 기존 파티션의 유지 여부를 신중히 결정
  - 기존 파티션의 일부를 유지해야 하는 경우, `mklabel` 명령어를 절대 실행하면 안 됨 (시험 환경)

- 다음 파티션의 시작 위치는 이전 파티션의 끝 위치와 동일하게 지정해야 함

**파티션 생성 및 포맷**
- `parted /dev/장치 mkpart primary xfs 2048s 1000MB`
  - 장치 이름 확인 : `lsblk`
- `mkfs.xfs /dev/파티션`
- `mkfs.ext4 /dev/파티션`

**`/etc/fstab` 추가**
- UUID 확인 : `lsblk --fs`, `blkid`

**적용**
- `systemctl daemon-reload`
- `mount -a`

<hr>

### 스왑 공간 관리

**스왑 상태**
- 유휴(Idle) : 현재 사용 빈도가 낮음
- 불필요(Unused) : 프로세스가 더 이상 필요로 하지 않음, 사용이 끝남
- 해제(Free) : 커널이 회수하여 다른 용도로 사용할 수 있는 RAM
- → Swap에는 Idle 상태의 메모리가 저장됨

**스왑 추가 방법**
- 스왑 파일로 추가 (예 : `/swap/swapfile`)
- 스왑 파티션으로 추가 (예 : `/dev/vdb1`)
- 스왑 LV로 추가 (예 : `/dev/vg1/swap1`) (시험 문제)

**스왑 생성**
- `parted -s /dev/sdb mkpart primary linux-swap 1001MB 1257MB`
- `parted -s /dev/sdb mkpart primary 1001MB 1257MB set 1 swap on`
- `udevadm settle`
- `mkswap /dev/파티션`

**스왑 공간 활성화**
- `/etc/fstab`
  - `UUID=? swap swap defaults 0 0`
- `swapon /dev/파티션`
- `swapon -a`

**스왑 공간 확인**
- `swapon -s`

<hr>

### `/etc/fstab` 오류 처리

- 루트 비밀번호를 알 경우
  - `mount` : `/`가 `rw`인지 `ro`인지 확인
  - `ro`일 시 : `mount -o rw,remount /`
  - 이후 : `vi /etc/fstab` 수정