### Linux 파일 권한

**Linux 파일 시스템 권한**
- `r (read)` : 파일 내용을 읽을 수 있습니다 / 디렉토리 내용을 표시할 수 있습니다
- `w (write)` : 파일 내용을 변경할 수 있습니다 / 디렉토리에 임의 파일을 생성하거나 삭제할 수 있습니다
- `x (execute)` : 파일을 명령처럼 실행할 수 있습니다 / 현재 작업 디렉토리가 될 수 있습니다 (단, 읽기 권한이 있어야 디렉토리 내용을 표시할 수 있음)

<hr>

### 파일과 디렉토리 권한 및 소유권 보기

**`ls` 명령**
- `-l` : 권한과 소유권에 대한 자세한 정보 표시 (파일유형, 권한, 소유자, 그룹 등)
- `-d` : 디렉토리의 내용이 아니라 디렉토리 자체에 대한 정보 표시

**파일 유형**
- `-` : 일반 파일
- `d` : 디렉토리
- `l` : 심볼릭 링크
- `c` : 문자 장치 파일
- `b` : 블록 장치 파일
- `p` : 파이프 파일
- `s` : 로컬 소켓 파일

<hr>

### 파일 권한 관리

**파일 및 디렉토리 권한 변경**
- `chmod (u,g,o,a)(+,-,=)(rwx) 파일/디렉토리`
- `chmod 777 파일/디렉토리`
- `-R` : 하위 파일/디렉토리 모두 적용

<hr>

### 사용자 또는 그룹 소유권 관리

**사용자 변경**
- `chown 사용자 파일/디렉토리`
  - `-R` : 하위 파일/디렉토리 모두 적용

**그룹 변경**
- `chown :그룹 파일/디렉토리`
- `chgrp 그룹 파일/디렉토리`

**사용자·그룹 동시 변경**
- `chown 사용자:그룹 파일/디렉토리`

<hr>

### 특수 권한

- `u+s` (suid, 4) : 파일이 소유한 사용자 권한으로 실행됨 (디렉토리는 영향 없음)
- `g+s` (sgid, 2) : 파일이 소유한 그룹 권한으로 실행됨 / 디렉토리에 생성된 파일은 디렉토리의 그룹 소유자와 일치함
- `o+t` (sticky, 1) : 파일은 영향 없음 / 디렉토리에 쓰기 권한이 있어도 자신이 소유한 파일만 삭제 가능 (다른 사용자 소유 파일은 삭제·이름변경 불가)

<hr>

### 기본 권한

- `umask` : 새로 생성되는 파일 및 디렉토리의 기본 권한을 제어하는 설정값 (기본값은 보통 `0022`)
- 파일 생성 시 최대 권한(8진수) : `0666`
- 디렉토리 생성 시 최대 권한(8진수) : `0777`

**설정 확인/변경 위치**
- `/etc/login.defs`
- `/etc/bashrc`
- `~/.bashrc` 에서 재정의 가능