### 시스템 로그 아키텍처

- `systemd-journald` : 이벤트 메시지 수집
- `rsyslog` : `systemd-journald` 서비스가 받은 syslog 메시지 수집

<hr>

### Syslog 이벤트 해석 및 관리

- 시스템에 이벤트 기록

**구성**
- `/etc/rsyslog.conf`
- `/etc/rsyslog.d/*.conf`

**설정**
- `*.유형 저장_위치`
- `*.debug /var/log/messages-debug` (예제)

<hr>

### 로그 파일 순환

**구성**
- `/etc/logrotate.conf`
- `/etc/logrotate.d/*.conf`

<hr>

### 로그 항목 분석

**로그 메시지 구성**
- 로그 타임스탬프 / 로그 메시지를 전송한 호스트 / PID 번호 / 로그 메시지

**tail 명령어 사용하기**
- `tail -f /var/log/messages /var/log/secure`
- `tail -f /var/log/messages | grep -E "fail|warn|error|crit|alert|emerg"`

<hr>

### 수동 로그 메시지 전송

- `logger`
  - `logger 메시지`
  - `logger -p 이름.유형 메시지`

<hr>

### 시스템 저널 로그

**장애 처리**
- `journalctl -f -u UNIT`
- `journalctl -p warn..err`
- (추가)
  - `systemctl --failed`
  - `systemctl list-jobs`

**구성**
- `/run/log/journal/*`

<hr>

### 시스템 저널 스토리지

**영구 시스템 저널 구성**
- `mkdir /var/log/journal`
- `journalctl --flush`

<hr>

### 로컬 시계 및 시간대 관리

- `timedatectl`
  - `timedatectl`
  - `timedatectl list-timezones`
  - `timedatectl set-timezone (이전 명령어에서 찾은 타임존)`

**NTP 동기화 해제**
- `timedatectl set-ntp false`

<hr>

### Chronyd 서비스 구성 및 모니터링 (중요 : 시험 1 문제)

**구성**
- `/etc/chrony.conf`
- `pool/server 주소 iburst`
- `systemctl restart chronyd`

- pool : 여러 대의 NTP 서버로 구성된 서버 풀(pool)을 가리킬 때 사용  
- server : 특정 NTP 서버 한 대를 직접 지정할 때 사용  