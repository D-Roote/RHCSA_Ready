!! 중요 Systemd 타이머 유닛
 - Systemd 타이머
    : .timer로 끝나는 유닛 구성 파일로 정의

 - Systemd 타이머 유닛 시작 및 중지
    $ systemctl start/stop UNIT

 - Systemd 타이머 유닛 활성화 및 비활성화
    $ systemctl enabe/disable UNIT

 - 시스템 구성 파일
    /usr/lib/systemd/system/*.type
    /etc/systemd/system/*.type

 - 타이머 유닛 구성 파일
    - [Unit]

    - [Timer]
    $ OnCalendar=[요일] [년]-[월]-[일] [시]:[분]:[초]
    "/N" : N 간격
    "," : 복수 옵션

    $ OnBootSec=
    $ OnUnitActiveSec=
    etc..

    - [Install]
    $ WantedBy=name.service     etc...

 - 타이머 유닛 변경 적용
    $ systemctl daemon-reload
    $ systemctl restart UNIT && systemctl enable UNIT

임시 파일 관리
 - Systemd-tempfile 툴
    : 임시 디렉토리/파일을 체계적이고 구성 가능한 방법을 제공하는 기능

 - 시스템 구성 파일
    /etc/tmpfiles.d/*.conf << 우선순위 가장 높음
    /usr/lib/tmpfiles.d/*.conf
    /run/tmpfiles.d/*.conf

 - *.conf 파일 구성
    $ d/D 디렉토리 권한 소유자 그룹 기간
    - d : 디렉토리가 없는 경우 생성 / 디렉토리가 있으면 다른 작업 수행 X
    - D : 디렉토리가 없는 경우 생성 / 디렉토리가 있으면 모든 내용 제거

    $ L 대상 권한 소유자 그룹 기간 목표
    - L : 심볼릭 링크 생성

 - Systemd 타이머 사용
    $ systemd-tmpfiles --clean (*.conf 파일) : 설정에 따라 정리
    $ systemd-tmpfiles --create (*.conf 파일) : 설정에 따라 생성

 - Systemd-tempfiles 타이머 정보
    $ systemctl cat systemd-tmpfiles-clean.timer

Cron을 사용한 반복 실행
 - 구성
    /etc/crontab/*
    
    관리자 : /etc/cron.d/*
    사용자 : 
    $ crontab -e
    /var/spool/cron/USER

 - 설정 작성
    분 시 일 월 요일 사용자 명령어

Anacron을 사용한 반복 실행
 - 구성
    /etc/anacrontab

 - 설정 작성
    Period in days, delay in minutes, job-identifier, command



시험 예제 (Timer 구성)

1. 스크립트 작성

cat << 'EOF' > /usr/local/bin/notify.sh
$!/bin/bash
logger "Time To Break!"
EOF
chmod +x /usr/local/bin/notify.sh

2. service 파일 작성
cat << 'EOF' > /etc/systemd/system/notify.service
[Unit]
Description=Notify Break Time Service
[Service]
Type=oneshot
ExecStart=/usr/local/bin/notify.sh
EOF

3. timer 파일 작성
cat << 'EOF > /etc/systemd/system/notify.timer
[Unit]
Description=Notify Break Time Timer
[Timer]
OnCalendar=*-*-* 07:00:00
Persistent=true
[Install]
WantedBy=timers.target
EOF

4. 서비스 실행
systemctl daemon-reload
systemctl enable notify.service
systemctl enable notify.timer