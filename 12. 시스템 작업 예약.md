### !! 중요 Systemd 타이머 유닛

- Systemd 타이머 : `.timer`로 끝나는 유닛 구성 파일로 정의

<hr>

### Systemd 타이머 유닛 시작 및 중지

- `systemctl start UNIT` / `systemctl stop UNIT`

<hr>

### Systemd 타이머 유닛 활성화 및 비활성화

- `systemctl enable UNIT` / `systemctl disable UNIT`

<hr>

### 시스템 구성 파일

- `/usr/lib/systemd/system/*.type`
- `/etc/systemd/system/*.type`

<hr>

### 타이머 유닛 구성 파일

**[Unit]**
- 유닛 설명 등 공통 메타 정보

**[Timer]**
- `OnCalendar=[요일] [년]-[월]-[일] [시]:[분]:[초]`
  - `/N` : N 간격
  - `,` : 복수 옵션 지정
- `OnBootSec=` : 부팅 후 경과 시간 기준 실행
- `OnUnitActiveSec=` : 유닛이 마지막으로 활성화된 시점 기준 경과 시간 실행
- 그 외 다수 옵션 존재

**[Install]**
- `WantedBy=timers.target` 등

<hr>

### 타이머 유닛 변경 적용

- `systemctl daemon-reload`
- `systemctl enable --now UNIT` : 부팅 시 자동 실행 등록 + 즉시 시작
  - (참고: `.timer` 유닛에 적용. 대상 `.service`는 직접 enable/start 하지 않음 — 타이머가 트리거)

<hr>

### 임시 파일 관리

- **systemd-tmpfiles 툴** : 임시 디렉토리/파일을 체계적이고 구성 가능한 방법으로 관리하는 기능

**시스템 구성 파일 (우선순위 순)**
- `/etc/tmpfiles.d/*.conf` ← 우선순위 가장 높음
- `/run/tmpfiles.d/*.conf`
- `/usr/lib/tmpfiles.d/*.conf`

**`*.conf` 파일 구성**
- `d/D 디렉토리 권한 소유자 그룹 기간`
  - `d` : 디렉토리가 없는 경우 생성 / 있으면 다른 작업 수행 안 함
  - `D` : 디렉토리가 없는 경우 생성 / 있으면 내부 내용 모두 제거
- `L 대상 권한 소유자 그룹 기간 목표`
  - `L` : 심볼릭 링크 생성

**systemd-tmpfiles 사용**
- `systemd-tmpfiles --clean *.conf` : 설정에 따라 정리
- `systemd-tmpfiles --create *.conf` : 설정에 따라 생성

**systemd-tmpfiles 타이머 정보**
- `systemctl cat systemd-tmpfiles-clean.timer`

<hr>

### Cron을 사용한 반복 실행

**구성**
- `/etc/crontab`
- 관리자용 : `/etc/cron.d/*`
- 사용자용 :
  - `crontab -e`
  - 실제 저장 위치 : `/var/spool/cron/USER`

**설정 작성 형식**
- `분 시 일 월 요일 사용자 명령어`
(단, `/etc/crontab`, `/etc/cron.d/*`처럼 시스템 crontab에는 "사용자" 필드가 포함되고, `crontab -e`로 편집하는 사용자 crontab에는 "사용자" 필드가 없음)

<hr>

### Anacron을 사용한 반복 실행

**구성**
- `/etc/anacrontab`

**설정 작성 형식**
`Period(days) Delay(minutes) Job-identifier Command`

<hr>

### 시험 예제 (Timer 구성)

**1. 스크립트 작성**
```bash
cat << 'EOF' > /usr/local/bin/notify.sh
#!/bin/bash
logger "Time To Break!"
EOF
chmod +x /usr/local/bin/notify.sh
```

**2. service 파일 작성**
```bash
cat << 'EOF' > /etc/systemd/system/notify.service
[Unit]
Description=Notify Break Time Service

[Service]
Type=oneshot
ExecStart=/usr/local/bin/notify.sh
EOF
```

**3. timer 파일 작성**
```bash
cat << 'EOF' > /etc/systemd/system/notify.timer
[Unit]
Description=Notify Break Time Timer

[Timer]
OnCalendar=*-*-* 07:00:00
Persistent=true

[Install]
WantedBy=timers.target
EOF
```

**4. 서비스 실행**
```bash
systemctl daemon-reload
systemctl enable --now notify.timer
```
> `.service`는 enable/start 불필요 — `.timer`가 시점이 되면 자동으로 호출함