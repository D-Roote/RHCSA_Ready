### 파일 시스템 마운트 및 마운트 해제

**수동으로 파일 시스템 마운트**
- `mount 장치 목표_디렉토리`
- `umount 목표_디렉토리`
- `lsof 디렉토리` : 디렉토리를 사용 중인 프로세스 표시

<hr>

### 파일 시스템에서 파일 찾기

**`locate` 사용**
- `updatedb` : 파일 인덱스 데이터베이스 갱신
- `locate -i 파일` : 인덱스 기반 검색 (`-i` : 대소문자 구분 없음)

**`find` 사용**
- `find 시작_주소 -name 파일`
- `-name 파일` : 이름으로 검색 (대소문자 구분)
- `-iname 파일` : 이름으로 검색 (대소문자 구분 없음)
- `-user 소유자` : 소유자 기준 검색
- `-group 그룹` : 그룹 기준 검색
- `-uid UID` : UID 기준 검색
- `-gid GID` : GID 기준 검색
- `-size (+/-)숫자(k/M/G)` : 용량 기준 검색
- `-mmin (+/-)숫자` : 분 단위 기준, 최근 변경된 파일 검색
- `-type d/l/b/c/p/s/f` : 파일 유형 기준 검색
- `-links 숫자` : 하드 링크 수 기준 검색

**`grep`으로 파일 내용 검색**
- `grep -ir 문자 시작_디렉토리`