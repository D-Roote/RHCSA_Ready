### 부팅 과정

**F/W 단계 - POST 과정**
- BIOS/UEFI 펌웨어

**BootLoader 단계 - GRUB2 (`grub.cfg`)**
- (BIOS) `/boot/grub2/grub.cfg`
- (UEFI) `/boot/efi/EFI/$LINUX/grub.cfg`
- `grub2-mkconfig`
- `grub2-install`
- `grubby`

**Kernel 단계 - `/etc/sysctl.conf` (`/etc/sysctl.d/*.conf`)**
- kernel → systemd 실행

**systemd 단계**

<hr>

### grubby

- GRUB 설정에서 Linux 커널 부팅 옵션을 다루는 도구
- `grubby --info=ALL` (또는 `--info=인덱스`) : 커널 정보 조회
- `grubby --set-default-index 0` : 기본 부팅 커널 인덱스 지정
- `grubby --update-kernel /boot/vmlinuz-... --args="옵션" / --remove-args="옵션"` : 커널 부팅 옵션 추가/제거

<hr>

### 부팅 프로세스 탐색 및 부팅 타겟 선택

<hr>

### systemd 타겟

**종속성 확인**
- `systemctl list-dependencies graphical.target | grep target`

**목록 확인**
- `systemctl list-units --type=target --all`

**런타임 타겟 설정**
- `systemctl isolate multi-user.target` (유닛 파일에 `AllowIsolate=yes` 옵션 필요)
- 고정(영구) 설정
  - `systemctl set-default multi-user.target`

**부팅 시 타겟 설정**
- 커널 명령줄에 추가
  - `systemd.unit=rescue.target`

<hr>

### 부팅 시 손상된 파일 시스템 복구

**파일 시스템 손상 확인 및 복구**
- `xfs_repair 블록장치`
- `fsck.ext4 -p 블록장치`

**emergency 부팅 타겟**
- `mount` : `/`가 `rw`인지 여부 확인
- `mount -o rw,remount /`
- `mount -a` : 오류 확인
- `/etc/fstab` 수정 후 `systemctl daemon-reload`